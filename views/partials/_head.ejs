<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Lab52 庫存管理</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
<style>
  body { background: #f5f6fa; }
  .sidebar { width: 220px; min-height: 100vh; background: #1a2035; }
  .sidebar .nav-link { color: #a0aec0; padding: .6rem 1.2rem; border-radius: 6px; margin: 2px 8px; }
  .sidebar .nav-link:hover, .sidebar .nav-link.active { background: #2d3a5c; color: #fff; }
  .sidebar .brand { font-size: 1.1rem; font-weight: 700; color: #fff; padding: 1.2rem; border-bottom: 1px solid #2d3a5c; }
  .main-content { flex: 1; padding: 1.5rem 2rem; }
  .card { border: none; border-radius: 12px; box-shadow: 0 1px 8px rgba(0,0,0,.07); }
  .stat-card { border-left: 4px solid; }
  .stat-card.blue  { border-color: #4e73df; }
  .stat-card.green { border-color: #1cc88a; }
  .stat-card.red   { border-color: #e74a3b; }
  .stat-card.yellow{ border-color: #f6c23e; }
  .badge-exp { font-size: .75rem; }
  .table th { background: #f8f9fc; font-weight: 600; font-size: .85rem; }
  .page-title { font-size: 1.3rem; font-weight: 700; color: #2d3a5c; margin-bottom: 1.2rem; }
</style>
</head>
<body>
<div class="d-flex">
  <!-- Sidebar -->
  <div class="sidebar d-flex flex-column">
    <div class="brand"><i class="bi bi-box-seam me-2"></i>Lab52 庫存</div>
    <nav class="nav flex-column mt-2">
      <a class="nav-link <%= page==='dashboard' ? 'active':'' %>" href="/"><i class="bi bi-speedometer2 me-2"></i>總覽</a>
      <a class="nav-link <%= page==='products' ? 'active':'' %>"  href="/products"><i class="bi bi-boxes me-2"></i>庫存管理</a>
      <a class="nav-link" href="/movements/new"><i class="bi bi-plus-circle me-2"></i>新增異動</a>
      <a class="nav-link <%= page==='billing' ? 'active':'' %>"   href="/billing"><i class="bi bi-receipt me-2"></i>帳單紀錄</a>
      <a class="nav-link <%= page==='import' ? 'active':'' %>"    href="/import"><i class="bi bi-cloud-download me-2"></i>匯入資料</a>
    </nav>
  </div>
  <!-- Main -->
  <div class="main-content">
